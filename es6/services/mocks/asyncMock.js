const asyncMock = {
};

const spyOnAsyncServiceMock = () => {
  // spyOn(asyncMock, 'on').and.callThrough();
  // spyOn(asyncMock, 'off').and.callThrough();
};

export {asyncMock, spyOnAsyncServiceMock};
